# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'leaf-journal' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for leaf-journal

  target 'leaf-journalTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'leaf-journalUITests' do
    # Pods for testing
  end
  pod 'FSCalendar'
end
